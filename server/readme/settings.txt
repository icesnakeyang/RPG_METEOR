Settings
记录系统预设置的变量。

Settings：{
    matchDays：  任务匹配有效时间
}

任务匹配有效时间：
    任务发布后，或者匹配给一个用户后，等待用户处理的时间。
    默认7天。
    假定系统设置过期天数为7天。如果用户发布了一个任务，那从发布时间开始计算，7天后自动过期。
    但是，如果RPG秘书对该任务进行分配，那么应该从分配日期开始计算7天。
    所以，过期时间应该是最后一次分配时间开始计算，7天后过期。如果没有分配时间，就是发布时间后7天。
